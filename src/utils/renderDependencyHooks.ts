export const renderDependencyHooks = <T extends {}>(model: T) => {
  //   const keys = Object.keys(model);
  //   let result = "";
  //   keys.forEach((key) => {
  //     result += `const { ${key} } = useFetch${key}();`;
  //   });
  return "";
};
